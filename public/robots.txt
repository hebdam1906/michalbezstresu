User-agent: *
Allow: /

# 7.09.2026: usunięte wszystkie reguły Disallow. Powód: odwołanie do Google Ads
# w sprawie zawieszenia za „Circumventing Systems: Cloaking". Google wymienia
# „robot nie może dotrzeć do strony docelowej" jako jeden z przykładów cloakingu,
# więc nie chcemy mieć w serwisie ANI JEDNEJ ścieżki zablokowanej dla robotów.
#
# Strony, które wcześniej były tu blokowane (/dziekuje, /dziekuje-firmy, /panel,
# /dashboard), i tak nie trafią do indeksu — każda z nich ma w <head> znacznik
# `noindex, nofollow` (patrz Layout.astro, parametr `noindex`).
#
# ⚠️ To jest w dodatku POPRAWNIEJSZE niż poprzedni stan: `noindex` działa tylko
# wtedy, gdy robot może stronę pobrać i ten znacznik zobaczyć. Blokada w
# robots.txt sprawiała, że znacznik był dla Google niewidoczny.

Sitemap: https://www.michalbezstresu.pl/sitemap-index.xml
